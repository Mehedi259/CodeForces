#include <bits/stdc++.h>
 
using namespace std;
 
signed main() {
    
    ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
 
   /* int t;
    cin >> t;
    while (t--) {
    	int n, res = 0;
    	cin >> n;
    	for (int i = 1; i <= n; i++) {
    		int x; cin >> x;
    		res = __gcd(res, abs(x - i));
            cout << "res = " << res << endl;
    	}
    	cout << res << "\n";
    }
    */
       		//int x; cin >> x;
    		//int res = __gcd(0,0);
            //cout << "abs = " << abs(x - 1) << endl;
            //cout << "res = " << res << endl;
                		int x; cin >> x;
    		int res = __gcd(res, abs(x - 1));
            cout << "res = " << res << endl;

    
}